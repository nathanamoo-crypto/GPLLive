// REMOVED - this widget only ever rendered hardcoded mock data
// (DUMMY_LEADERBOARD) and had no real backend behind it. Replaced on Home by
// MotmVoteSpotlight.tsx. No longer imported anywhere; this file can be
// deleted from disk whenever convenient (left in place since this session
// doesn't have permission to delete files in this folder).
export {};
