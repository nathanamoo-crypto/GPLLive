const BASE_IP = '172.20.10.2';

export const API_URLS = {
  AUTH_URL: `http://${BASE_IP}:8081`,
  MATCH_URL: `http://${BASE_IP}:8082`,
  FANTASY_URL: `http://${BASE_IP}:8083`,
  VOTE_URL: `http://${BASE_IP}:8084`,
  PREDICTION_URL: `http://${BASE_IP}:8085`,
  NOTIFICATION_URL: `http://${BASE_IP}:8086`,
};
